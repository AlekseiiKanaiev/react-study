import React from 'react';

export const Loader = () => {
    return (
        <div>
            <div className="spinner-border">
                <span className="sr-only">Loading...</span>
            </div>
        </div>
    );
}